---
# master host
master_host: ""

# client group
client_group: ""

# nis domain
nisdomain: mydomain

# subnet_ip
subnet_ip: 10.0.0.0
# subnet_mask
subnet_mask: 255.255.255.0

# master_ip, required for clients. Leaving master_host empty should skip master installation
master_ip: 0.0.0.0
